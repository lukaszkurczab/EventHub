{
  "name": "eventhub-microfrontends",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=@eventhub/shell\" \"npm run dev --workspace=@eventhub/events\" \"npm run dev --workspace=@eventhub/host-dashboard\"",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify:shell": "npm run verify --workspace=@eventhub/shell",
    "verify:events": "npm run verify --workspace=@eventhub/events",
    "verify:host-dashboard": "npm run verify --workspace=@eventhub/host-dashboard",
    "verify:independence": "npm run verify:events && npm run verify:host-dashboard && node scripts/verify-independent-remotes.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
